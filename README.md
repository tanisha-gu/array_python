# array_python
A structured collection of Array DSA problems and solutions, covering everything from beginner to advanced level.


###  Level 1 — Very Basic

1. Create an array containing `1, 2, 3, 4, 5` and print it.

2. Print each element of an array using a `for` loop.

3. Find the length of an array.

4. Print the **first element** of an array.

5. Print the **last element** of an array.

6. Print the array in reverse order.

7. Find the sum of all elements in an array.

8. Find the largest element in an array.

9. Find the smallest element in an array.

10. Count how many elements are present in an array.

###  Level 2 — Basic Practice

11. Create an array and check whether `10` exists in it.

```python
from array import *

arr = array('i', [2, 5, 10, 15, 20])
```

12. Find the index of `10`.

13. Add a new element `25` to the array.

14. Insert `100` at index `2`.

15. Remove `10` from the array.

16. Reverse the array using `.reverse()`.

17. Copy one array into another array.

18. Count how many times `5` occurs.

19. Find the second-largest element.

20. Print only the even numbers from an array.

###  Level 3 — Interview Beginner

21. Print only the odd numbers.

22. Find the average of all elements.

23. Find the number of positive and negative numbers.

24. Find duplicate elements.

25. Remove duplicate elements.

26. Find the second-smallest element.

27. Search for an element **without using** `.index()`.

28. Find the sum of even numbers only.

29. Find the sum of odd numbers only.

30. Reverse an array **without using** `.reverse().
